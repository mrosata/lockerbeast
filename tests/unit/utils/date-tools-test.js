import dateTools from 'lockerbeast/utils/date-tools';
import { module, test } from 'qunit';

module('Unit | Utility | date tools');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = dateTools();
  assert.ok(result);
});
