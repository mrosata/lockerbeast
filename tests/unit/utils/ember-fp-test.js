import emberFp from 'lockerbeast/utils/ember-fp';
import { module, test } from 'qunit';

module('Unit | Utility | ember fp');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = emberFp();
  assert.ok(result);
});
