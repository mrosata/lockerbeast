import { ifLte } from 'lockerbeast/helpers/if-lte';
import { module, test } from 'qunit';

module('Unit | Helper | if lte');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = ifLte([42]);
  assert.ok(result);
});
