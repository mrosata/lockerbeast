import { table } from 'lockerbeast/helpers/table';
import { module, test } from 'qunit';

module('Unit | Helper | table');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = table([42]);
  assert.ok(result);
});
