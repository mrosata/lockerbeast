import { eitherOr } from 'lockerbeast/helpers/either-or';
import { module, test } from 'qunit';

module('Unit | Helper | either or');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = eitherOr([42]);
  assert.ok(result);
});
