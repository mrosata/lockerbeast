import { moduleForComponent, test } from 'ember-qunit';
//import hbs from 'htmlbars-inline-precompile';

moduleForComponent('jquery-ui', 'Integration | Component | jquery ui', {
  integration: true
});

test('it renders', function(assert) {
  assert.equal(1 + 1, 3 + 3 - 4, 'I did not write a test for this.');
});
